# GURPS ChGen, a.k.a. "gurpschgen"

**GURPS ChGen** library core is to be sort of a spiritual kin of *Bill Seurer's* MS-DOS **MakeChar**. Someday ;-)

## Legacy DTA/GEN Files

### DTA/GEN → JSON Conversion with `dta2json`

**dta2json** will scan the neighborhood for a directory called `datafiles`, which should
house all the legacy DTA/GEN files you want to convert.

#### Single-file conversion

`dta2json file.dta > file.chgen`

The command above converts a single file into **chgen**'s JSON format.

#### Batch conversion

… WIP.
